<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-Mapper.dtd">
  
  <mapper namespace="com.mycom.myweb.mapper.BoardMapper">
  
  <select id="getTotalCount" resultType="int">
    SELECT COUNT(*) FROM board
  </select>

  <select id="getListWithPaging" resultType="com.mycom.myweb.BoardVO">
  <![CDATA[
    SELECT bno, title, content, writer, regdate, updatedate
    FROM (
        SELECT /*+INDEX_DESC(board pk_board) */
          rownum rn, bno, title, content, writer, regdate, updatedate
        FROM board
        WHERE rownum <= #{pageNum} * #{amount}
    )
    WHERE rn > (#{pageNum} - 1) * #{amount}
]]>
  </select>

  <select id="get" resultType="com.mycom.myweb.BoardVO">
   SELECT * FROM board WHERE bno = #{bno}
  </select>

  <insert id="insert">
    INSERT INTO board (bno, title, content, writer)
    VALUES (board_seq.nextval, #{title}, #{content}, #{writer})
  </insert>

  <update id="update">
     UPDATE board SET title = #{title}, content = #{content}, updatedate = sysdate
     WHERE bno = #{bno}
  </update>

  <delete id="delete">
     DELETE FROM board WHERE bno = #{bno}   
  </delete>

  <select id="getList" resultType="com.mycom.myweb.BoardVO">
      SELECT * FROM board WHERE bno > 0 ORDER BY bno DESC
  </select>

</mapper>














